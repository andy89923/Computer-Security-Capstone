CXX=g++

# APP_NAME=mitm_attack pharm_attack
# FILE_NAME=mitm_attack.cpp pharm_attack.cpp 

# $(APP_NAME): $(FILE_NAME)
# 	$(CXX) -o $(APP_NAME) $(FILE_NAME)

# test: test.cpp
# 	$(CXX) -o test.o test.cpp

clean:
	sudo systemd-resolve --flush-caches
	rm -f $(APP_NAME) *.o
